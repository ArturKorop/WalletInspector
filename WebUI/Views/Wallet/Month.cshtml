@model Domain.Code.Time.Month
@section featured {
    <div>
        <p>@Model.Name</p>
        <p>@Model.MonthNumber</p> 
        <table>
            @foreach (var day in Model.Days)
            {
                Html.RenderPartial("Day", day);
            }
        </table>
    </div>
}