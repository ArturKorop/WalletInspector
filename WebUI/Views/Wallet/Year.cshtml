@model Domain.Code.Time.Year

@section featured {
    <div id="YearMain">
        <div>@Model.Name</div>
        <div id="MonthList">
            @{ var monthNumber = 0; }
            @for (int i = 0; i < 3; i++)
            {<div id="GrouptMonthStat">
                 @for (int j = 0; j < 4; j++)
                 {
                     <div style="display: inline-block;margin: 1em;padding: 0.5em" class="MonthStat">
                         @{
                             monthNumber++;
                             var currentMonth = Model.GetMonth(monthNumber);
                         }
                         @Html.Partial("MonthStatistic", currentMonth)
                     </div>
                 }
             </div>
            }
        </div>
        <div id="TotalYearSum">
            Total: @Model.Statistic.GetYearTotalSum()
        </div>
    </div>
}
